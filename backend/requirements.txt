fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pdfplumber==0.11.4
python-multipart==0.0.12
pydantic==2.9.2
pytest==8.3.3
httpx==0.27.2
# PostgreSQL driver — required when DATABASE_URL points to Supabase
psycopg2-binary==2.9.10
# JWT validation — verifies Supabase auth tokens on every protected API request
python-jose[cryptography]==3.3.0
# Loads .env file so we don't hard-code secrets in the code
python-dotenv==1.0.1
# Stripe payment processing (Phase 3)
stripe==11.4.0
# Error tracking — catches crashes in production and sends them to Sentry
sentry-sdk[fastapi]==2.19.2
# Receipt OCR — Gemini is the default provider (1500 free requests/day);
# Anthropic Claude is kept as a fallback. The receipt_ocr service picks
# whichever has its API key set, preferring Gemini for the cost win.
google-generativeai==0.8.3
anthropic==0.40.0
# Pillow — used to validate and downscale uploaded receipt images before
# sending them to the OCR provider (smaller payload, faster + cheaper).
Pillow==11.0.0
