fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pdfplumber==0.11.4
python-multipart==0.0.12
pydantic==2.9.2
pytest==8.3.3
httpx==0.27.2
# PostgreSQL driver — required when DATABASE_URL points to Supabase
psycopg2-binary==2.9.10
# JWT validation — verifies Supabase auth tokens on every protected API request
python-jose[cryptography]==3.3.0
# Loads .env file so we don't hard-code secrets in the code
python-dotenv==1.0.1
# Stripe payment processing (Phase 3)
stripe==11.4.0
# Error tracking — catches crashes in production and sends them to Sentry
sentry-sdk[fastapi]==2.19.2
# Anthropic SDK — used by the receipt OCR endpoint to parse photos via Claude
# vision. Pinned to a recent version that supports vision messages and the
# Sonnet 4.6 / Opus 4.7 model family.
anthropic==0.40.0
# Pillow — used to validate and downscale uploaded receipt images before
# sending them to Claude (smaller payload, faster + cheaper).
Pillow==11.0.0
